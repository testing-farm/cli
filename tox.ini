[tox]
isolated_build = true

[testenv]
sitepackages = False
skip_install = true
whitelist_externals = poetry
commands_pre = poetry install -v
passenv = POETRY_ADDOPTS TERMINFO TERM

[testenv:type-check]
commands = pyre check
